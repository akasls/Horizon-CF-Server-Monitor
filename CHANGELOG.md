# 更新日志 (Changelog)

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/) 版本规范。

---

## [v1.4.0] - 2026-09-08

### 🚀 全面支持扩展自定义监控节点与动态缺失过滤
- **适配 `/api/config` 最新自定义节点名称**：
  - 新增支持 `node_1_name`、`node_2_name`、`node_3_name`、`node_4_name` 4 大自定义节点配置解析与回退机制。
  - 结合已有 `custom_ct_name`、`custom_cu_name`、`custom_cm_name`、`custom_bd_name`，组成全量 8 监控节点统一名称获取引擎。
- **单机详情页历史图表与规格面板深度适配**：
  - 动态对接 `/api/history/all` 与 `/api/server` 下发的 `ping_node_1~4` 与 `loss_node_1~4` 监控数据。
  - **精准动态存在性过滤**：若某节点数据不存在（包括 `ping_bd` 等旧字段及 `node_1~4` 为空/null/false），图表折线、图例及面板中**坚决不展示**，彻底告别空指标与无效连线。
  - **图表 Tooltip 与图例体验重构**：鼠标悬停采样点同时展示实时延迟与丢包率（`ms · 丢包 x%`），8 节点独立色彩高对比度渲染。
  - **网络与连接规格面板**：实时动态列出所有已配置节点的当前延迟与丢包率。
- **首页卡片自适应渲染**：
  - 卡片底部延迟指标网格完全动态化，仅渲染实际存在有效延迟的节点，网格列数自适应；若无节点配置则自适应隐藏分隔栏。
- **离线与演示数据完善**：
  - `generateDemoData()` 与 `generateMockHistory()` 补齐 8 节点全套数据模拟。

---

## [v1.3.0] - 2026-09-07

### 🌐 支持自定义网络线路/节点名称
- **适配 `/api/config` 最新自定义字段**：
  - 支持读取后端下发的 `custom_ct_name`（电信）、`custom_cu_name`（联通）、`custom_cm_name`（移动）与 `custom_bd_name`（BGP）字段。
  - **优雅降级与版本兼容**：当后端未返回对应字段或字段为空时，自动平滑回退至多语言字典的原始内置线路名称（如“电信/联通/移动/BGP”或“CT/CU/CM/BGP”），100% 兼容旧版 CF-Server-Monitor。
- **全链路动态渲染应用**：
  - **服务器卡片**：底部 4 列并排延迟指示器动态采用自定义线路名称，增加文本超长省略保护与原生 Tooltip 悬浮提示。
  - **单机详情页**：线路延迟历史图表（Ping & Loss）的系列折线标签与交互图例动态同步自定义线路名称。
  - **本地演示数据**：Demo 模式同步补全 4 大线路自定义属性，保证离线与开发预览一致性。

---

## [v1.2.0] - 2026-09-03

### 🛡️ 兼容性增强与错误状态重构
- **废除线上静默 Demo 覆盖机制**：
  - 严禁在线上探针环境因接口异常静默将真实站点替换为内置演示卡片。
  - 严格限制演示模式仅在显式携带 `?demo=1` / `#demo` 或本地文件离线调试（`file://` 且未配后端）时激活。
- **细化 HTTP 状态分流与可视化操作引导**：
  - **私有站点（401 Unauthorized）**：优雅展示「私有监控面板」安全图标，并提供「登录管理后台」直达按钮（`/admin#admin`）。
  - **人机验证（403 Forbidden）**：接入 Cloudflare Turnstile 验证弹层与「重新验证」交互，杜绝验证阶段报错导致崩溃。
  - **数据库升级（409 Conflict）**：提示管理员前往后台执行数据库迁移维护。
  - **网络或服务器异常**：控制台保留完整排查日志，界面提供「点击重试」按钮。
- **空服务器与过滤状态解耦**：
  - 站点节点总数为 0 台时提示「暂无在线服务器，请在管理后台添加」，区分搜索无结果状态。
- **统计指标防御性加固**：
  - 全局统计指标提取全面引入 `safeNum` 与可选链保护，杜绝因第三方探针缺省字段导致的 `NaN` 或未捕获运行时错误。

---

## [v1.1.0] - 2026-08-18

### 🚀 移动端交互与自适应重构
- **分类导航与搜索框响应式重构**：
  - 移动端分类标签支持横向顺滑滑动（`-webkit-overflow-scrolling: touch`），收紧分类项与分隔符 `|` 的外边距。
  - 移动端搜索框默认折叠为右侧搜索图标，点击平滑展开全宽搜索条并自动聚焦；支持点击关闭图标或按 `Esc` 键一键清空并收起。
- **详情页头部与返回按钮对齐**：
  - 详情页标题与「返回列表」按钮锁定处于同排右上角，单机名称过长自动文字截断省略，杜绝折行混乱。
- **历史图表移动端横向溢出根治**：
  - 彻底移除 SVG 图表上的强制 `min-height`，解决移动端因高宽比强制拉大 SVG 宽度至 788px 撑爆屏幕的缺陷。
  - 容器添加 `min-width: 0; max-width: 100%` 双重安全边界保护，完美自适应小屏手机。
- **图例与实时指标单行防折行**：
  - 将图例名称精简为高信息密度短标签（`CPU`、`RAM`、`下行`、`上行`、`电信`、`联通`、`移动`、`BGP`、`读`、`写`）。
  - 底部栏强制 `flex-wrap: nowrap`，确保左侧图例与右侧实时百分比（如 `CPU: 18.5% · RAM: 34.2%`）在任何窄屏手机下牢牢保持在同一行。

### 🎨 动效与视觉体验调优
- **主题波浪扩散动效丝滑度优化**：
  - 扩散半径终点计算增加安全外扩（`+32px`），确保全屏四角 100% 覆盖。
  - 优化贝塞尔缓动曲线为 `cubic-bezier(0.2, 0, 0, 1)`（380ms），添加 `fill: 'forwards'` 状态保持，彻底消除过渡结束时伪元素销毁瞬间的微卡顿感。
- **资产价值统计升级为「小鸡残值」**：
  - 顶部资产卡片更名为「小鸡残值」，上方大字直接展示全网折合后的**剩余价值**总额。
  - 下方副标题展示**总购入价值**与**每月续费折合**（如 `总价值 ¥ 598 · ¥ 142/月`），剔除冗余的「共 X 台」文案。
- **当前在线图标更换**：
  - 替换原先不符合监控主题的箭头图标为契合探针主题的 **双层机架服务器指示灯图标（Server Rack with LED Indicators）**。

### ⚡ 格式化与数值智能展示
- **网络速率智能整数化**：
  - 在 `B/s`、`KB/s`、`MB/s` 级别自动四舍五入为纯整数（例如 `13.8 KB/s` 展现为 `14 KB/s`，`14.1 MB/s` 展现为 `14 MB/s`），界面更纯净干练。
  - 达到 `GB/s` 及以上大流量时才保留 1 位小数（如 `1.2 GB/s`）。
- **流量与存储消除无效 `.0` 尾随小数**：
  - 针对所有单位智能消除无效小数位，例如 `2.0 TB / 20.0 TB` 自动呈现为 `2 TB / 20 TB`，`2.0G` 呈现为 `2G`；
  - 含有非零有效小数（如 `1.7 MB`、`13.8 KB`）时原样精准保留。

### 🛠️ 新增定制组件
- **悬浮返回顶部按钮**：
  - 右下角新增 `#btn-back-to-top` 悬浮按钮，当页面向下滚动超过一屏时丝滑淡入浮现，点击平滑滚动回到顶部。
- **详情页定制时间筛选下拉组件**：
  - 废弃浏览器原生 `<select>` 弹层，重构为与 Horizon 扁平极简风 100% 契合的定制下拉组件（`.custom-dropdown`）。
  - 下拉展开菜单与触发按钮 100% 等宽对齐（80px），容器重置为 `overflow: visible` 解决被卡片截断问题，支持点击外部区域或按 `Esc` 键自动关闭。

---

## [v1.0.0] - 2026-08-17

- 初始版本发布：支持 CF-Server-Monitor 完整探针功能。
- 扁平极简仪表盘设计、三维圆环图表、多运营商延迟平行排版。
- 秒级 WebSocket 实时推送、离线降级轮询、Cloudflare Turnstile 验证适配。
- 多币种自动换算、小鸡价值/剩余价值计算引擎。
